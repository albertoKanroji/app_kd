import 'package:flutter/material.dart';

class ClientDeliveryListPage extends StatelessWidget {
  const ClientDeliveryListPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
    body: Center(
      child:Text('Orders'),
    ),
  );
  }
}